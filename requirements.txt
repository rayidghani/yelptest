Flask>=3.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
Pillow>=10.0.0
# TensorFlow wheels differ by platform/architecture.
# Apple Silicon (arm64 macOS) should use tensorflow-macos to avoid AVX/x86 issues.
tensorflow-macos>=2.12.0; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow>=2.12.0; not (platform_system == "Darwin" and platform_machine == "arm64")
